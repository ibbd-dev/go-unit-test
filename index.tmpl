<html>
    <div><h1>启动项目</h1></div>
    <div>
        <ul>
        {{range $k, $v := .projects}}
            <li><a href="http://{{$.host}}:{{$.port}}/action/{{$v.Name}}/show" target="_blank">{{$v.Name}}</a></li>
        {{end}}
        </ul>
    </div>
    <div>
        <p>注：按需启动相应的项目，点击之后会在新页面打开链接，正常会输出一个url，在新窗口打开url即可看到单元测试的状态。</p>
    </div>
</html>
